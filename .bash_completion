
if [ -e "$HOME/.local/share/bash_completion/completions" ]
then
    for entry in $(ls -1 "$HOME/.local/share/bash_completion/completions")
    do
        source "$HOME/.local/share/bash_completion/completions/$entry"
    done
fi
